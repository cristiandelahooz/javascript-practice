#!/usr/bin/env node

const BUTTOM = 97;
const TOP = 122;
const SHIFT_LENGTH = 3;

/**
 * @param {string} s
 * @param {number[][]} shifts
 * @return {string}
 */
shiftingLetters = (s, shifts) => {
  for (let i = 0; i < shifts.length; i++) {
    for (let value = 0; value < SHIFT_LENGTH; value++) {}
  }
};
